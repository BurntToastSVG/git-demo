NEW PROJECT GIT COMMANDS
    - "make sure to create repo on github.com"
    - "cd into project in terminal"

    - git init
    - git remote add origin GITLINK   (copy from new repo page)
    - git branch -M main



ADDING/UPDATE FILES TO REPO 
    git status                               (check for updates)
    git add .                                (add files to commit)
    git commit -m "COMMENT FOR THE UPLOAD"   (create commit with message)
    git push origin main                     (push the commit to github)






HOMEWORK 
    Please use chat gpt to update the readme then upload to github